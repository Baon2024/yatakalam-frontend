import AuthCard from "@/components/ui/authCard"




const Onboard = () => {


    //need to get auth logic here for signing up or loggin in
    //if successful, save login in useContext()
    //and if signing up, then generate a random num, and insert into user-details table, as their Unique Developer Number
    //after saving user in useContext, also save UDN in useContext(), for use in the endpoint url of PhoneForm





    return (
        <AuthCard />
    )
}





export default Onboard